{
  "name": "mern-project",
  "version": "1.0.0",
  "description": "Root package for MERN stack",
  "scripts": {
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
